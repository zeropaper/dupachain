{
  "name": "ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:dev": "esbuild --bundle src/dc-chat.ts --outfile=dist/dc-chat.js",
    "build": "esbuild --bundle src/dc-chat.ts --minify --outfile=dist/dc-chat.min.js",
    "vite:build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "lit": "^3.1.0",
    "@local/client": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.0.8"
  }
}